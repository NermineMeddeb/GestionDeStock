package Mts.Crud.Configuration; // Assurez-vous que le package correspond à votre projet

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenAPIConfig {

   /*  @Bean
    public GroupedOpenApi publicApi() {
        return GroupedOpenApi.builder()
            .group("public")
            .pathsToMatch("/**") // Cela inclut toutes les routes
            .build();
    }

    @Bean
    public OpenAPI customOpenAPI() {
        return new OpenAPI()
            .info(new Info().title("Gestion de stock MTS API").version("1.0")
                .description("Documentation de l'API REST pour l'application de gestion de stock."));
    }
                */
}
